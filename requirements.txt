# CompanionHeart-Backend 依赖
# 安装: pip install -r requirements.txt

# === Web 框架 ===
fastapi>=0.128.0
uvicorn>=0.40.0
pydantic>=2.0.0
annotated-doc>=0.0.4

# === LLM ===
openai>=1.0.0

# === TTS ===
genie-tts>=2.0.0
edge-tts>=6.0.0
pypinyin>=0.55.0
g2pM>=0.1.2
jieba_fast>=0.53
nltk>=3.9.0
pyopenjtalk-plus>=0.4.0

# === ASR ===
# 插件 custom_plugin/ASR_funasr 的依赖，安装注意事项见该目录的 requirements.txt：
#   - torch/torchaudio 装 CPU 版（--index-url https://download.pytorch.org/whl/cpu）
#   - funasr 清华镜像没有，需 PyPI 官方源
#   - 装完 funasr 必须回退 onnxruntime==1.22.1（否则 genie-tts 无法工作）
funasr>=1.3.29
torch>=2.0.0
torchaudio>=2.0.0
onnxruntime==1.22.1  # 版本被 genie-tts 2.0.2 锁定，勿升级

# === 工具 ===
PyYAML>=6.0
python-dotenv>=1.0.0
httpx>=0.28.0
python-multipart>=0.0.9  # FastAPI multipart 表单（ASR 音频上传）
